{
  "name": "@busy-office/runtime",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "description": "Determination + delivery runtime (Stage 3, HLD §2/§4): Event API ingress and contract validation. Single-process by default (CLAUDE.md: API + worker + embedded queue + FS archive in one command).",
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1119.0",
    "@busy-office/output-schema": "*",
    "@busy-office/render-typst": "*",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "nodemailer": "^9.0.6"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/nodemailer": "^8.0.1"
  }
}
