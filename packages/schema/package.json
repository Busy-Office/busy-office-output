{
  "name": "@busy-office/output-schema",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "description": "Contracts only: data contract, variant identity, renderer interface, draft document schema and Layout IR. Zero runtime dependencies — this package is imported by everything."
}
