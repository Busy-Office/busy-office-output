{
  "name": "busy-office-output",
  "private": true,
  "version": "0.0.0",
  "description": "ERP document output: determination, rendering, archive, delivery, audit. Stage 0.",
  "engines": { "node": ">=22" },
  "workspaces": ["packages/*"],
  "scripts": {
    "typecheck": "tsc --strict --noEmit --target es2022 --module nodenext --moduleResolution nodenext packages/schema/src/index.ts && tsc --strict --noEmit --target es2022 --module nodenext --moduleResolution nodenext packages/render-typst/src/index.ts && tsc --strict --noEmit --target es2022 --module nodenext --moduleResolution nodenext packages/render-typst/src/cli/diff.ts && tsc --strict --noEmit --target es2022 --module nodenext --moduleResolution nodenext packages/runtime/src/index.ts && tsc --strict --noEmit --target es2022 --module nodenext --moduleResolution nodenext packages/runtime/src/archive/index.ts",
    "test": "vitest run",
    "diff": "tsx packages/render-typst/src/cli/diff.ts",
    "bench:po": "tsx test/corpus/purchase-order/bench.ts",
    "verify": "npm run typecheck && npm test"
  },
  "devDependencies": { "typescript": "^5.6.0", "vitest": "^2.1.0", "tsx": "^4.23.0" }
}
