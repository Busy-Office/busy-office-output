{
  "name": "busy-office-output",
  "private": true,
  "version": "0.0.0",
  "description": "ERP document output: determination, rendering, archive, delivery, audit. Stage 0.",
  "engines": { "node": ">=22" },
  "scripts": {
    "typecheck": "tsc --strict --noEmit --target es2022 --module nodenext --moduleResolution nodenext packages/schema/src/index.ts",
    "test": "vitest run",
    "verify": "npm run typecheck && npm test"
  },
  "devDependencies": { "typescript": "^5.6.0", "vitest": "^2.1.0" }
}
