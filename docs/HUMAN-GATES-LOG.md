# Human gates log

Append-only. The loop logs a gate and continues; the human answers in
`docs/INBOX.md` (reference the gate ID). Status: open / answered / closed.

| Gate | Needs | Evidence for the human | Logged | Status |
|---|---|---|---|---|
| GATE-TYPST-INSTALL | `brew install typst` on this Mac | needed for gate-5 typst row + RTL/CJK typst column | 2026-08-26 | **answered** — typst 0.15.1 found on PATH at tick 5, gate-5 typst row + RTL/CJK typst column filled in RESULTS.md |
| GATE-BURST-WINDOW | the stated bursting window (minutes for 8,000 docs) | RESULTS.md bursting table will be filled per candidate window | 2026-08-26 | **closed** — maintainer chose 30 minutes 2026-08-27. Both renderers clear it single-process without worker-pool fan-out (pdf-direct 18.6x margin, Typst 2.25x margin); defers fan-out investment until a real SLA demands tighter. spike/RESULTS.md §Bursting math |
| GATE-CARBONE | author po-template.odt + LibreOffice, or write "skip carbone" in INBOX | RESULTS.md gate-matrix carbone column; blocks ADR-000 draft | 2026-08-26 | **closed** — maintainer chose "skip carbone" 2026-08-26 (docs/INBOX.md), not benchmarked. Rationale: LibreOffice runtime weight conflicts with "never gold-plate a renderer"; CCL license unreviewed and deferred. ADR-000 draft proceeds on pdf-direct + typst evidence only; Path B/Carbone stays optional behind the Renderer seam per HLD.md, revisited only if a named user needs .odt/.docx authoring |
| GATE-S1-PREWORK | may the loop do path-independent Stage 1 tasks while Stage 0 is human-blocked? default NO | see docs/LOOP-PLAN.md | 2026-08-26 | **closed** — maintainer answered directly in chat 2026-08-27: "Yes, start Stage 1," full authorization, not scoped to a subset, logged verbatim in docs/INBOX.md (Open section, this session). Superseded by circumstance: Stage 0's own exit gate is already 4/4 PASS (ROADMAP.md, 2026-08-27) and ADR-000/ADR-001 are both Accepted, so Stage 1 start no longer needs an early-start exception — this answer is read as a general go-ahead for all Stage 1 tasks. One carve-out stands independent of this gate: the Path B (Carbone) Stage 1 task stays out of scope per ADR-000's own "do not build both speculatively" clause, not because of this gate. Decision remains with the maintainer. |
| GATE-RTL-SHAPING | **superseded 2026-08-26 (tick 5)** — original tick-3 wording ("pdf-lib does no bidi/GSUB shaping") was wrong, see docs/RESULTS.md correction note; real gaps are narrower: (1) pdf-lib's TrueType subsetter breaks on large composite-glyph CJK fonts — full-font-embed workaround costs ~5.7MB/font; (2) pdf-lib has no font-fallback chain, so any font missing Latin/digit glyphs (common for OS "companion" scripts fonts) silently renders `.notdef` boxes when mixed with numbers — the normal shape of a real invoice. Typst has neither gap here. Decide: assemble complete-per-script font sets by hand for pdf-lib (no fallback), accept the CJK size cost, name Typst/a second renderer for non-Latin volume docs, or defer non-Latin scripts out of Stage 0 scope | docs/RESULTS.md § RTL / CJK smoke test (includes the correction writeup); `spike/` was deleted at Stage 0 close, evidence now lives only in docs/RESULTS.md — no longer independently reproducible via `npm run spike:rtl-cjk` | 2026-08-27 | **closed** — answered indirectly via ADR-001 (Accepted 2026-08-27), not a direct INBOX ruling on this gate ID. ADR-001 routes any document with non-Latin scripts to Typst (alongside multi-page layout and carry-forward totals), citing this gate's own evidence (no font-fallback chain, TrueType subsetter bug on composite-glyph CJK fonts) as the driver — this is option 3 ("name Typst/a second renderer for non-Latin volume docs"), superseding options 1/2/4. pdf-direct is not expected to handle non-Latin scripts going forward; no per-font-set assembly or CJK size-cost acceptance work is needed. Chair inference from ADR-001's text, not an explicit gate-ID citation by the maintainer — flagged here for the record. Decision remains with the maintainer. |
