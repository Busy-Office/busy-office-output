# Human gates log

Append-only. The loop logs a gate and continues; the human answers in
`docs/INBOX.md` (reference the gate ID). Status: open / answered / closed.

| Gate | Needs | Evidence for the human | Logged | Status |
|---|---|---|---|---|
| GATE-S5-CLOSE | **Stage 5 cannot close with ADR-007's two addenda still Proposed** — (a) the OutputPort v1 surface addendum, (b) the v1.1 amendment (reprint verbs `reproduce`/`regenerate`, `EmitInput.reissues`, plus the task-4 console-write line: `POST /output/templates/:id/:ver/review` writes only the lifecycle log, actor proxy-asserted via `resolveActor`). Accept both, amend, or reject — CLAUDE.md: "no stage closes with its ADRs open" | all five Stage 5 tasks are built and gate-checked, incl. task 5 (3a78ef9); the Stage 5 exit gate itself is **MET** (three independent proofs re-run at 3a78ef9 — pure table, through-the-port, through-the-screen) — see ROADMAP.md's Stage 5 exit-gate annotation and docs/SESSION-LOG.md 2026-08-29. One more open question folded in here: GAP-13's ratified exception text says the loop "may proceed into Stage 5" — it does not say Stage 5 may close or Stage 6 may start; read narrowly (build only) or broadly (build-and-close)? | 2026-08-29 | **closed** — maintainer ruled directly in chat, 2026-08-29: both ADR-007 addenda **Accepted as drafted**, no changes (ADRs/007-packaging-embedding.md, ADRs/README.md updated). GAP-13's exception text read **broadly** — it covers closing Stage 5 and starting Stage 6, not just the building that already happened; no separate Stage-6-start exception is needed. Stage 5 marked CLOSED in ROADMAP.md same date. |
| GATE-S5-RULINGS | Seven small rulings surfaced by Stage 5 gate-checks, bundled so one INBOX note answers them: (1) exit-gate wording — "cannot reach PRD without an approval record" read as *through the lifecycle* (GAP-19's S1 seed on FIRST registration stays the sole bypass) or *by any means* (S1 seed goes; `registerDocumentType` would refuse `published` as an initial state); (2) SoD scope — today two pairings (approver ≠ submitter, publisher ≠ approver), the SUBMITTER MAY PUBLISH their own approved change; keep, or require three distinct people; (3) `regenerate` writes the REPRINT row and the `reprint_log` row even when composition then fails (row stays not-archived, honestly reported) — accept, or log only after a rendered composition; (4) `STRANDED_AFTER_MS` = 5 min on the Overview (display-only; `serve()` sweeps at startup regardless) — ratify or change the one constant; (5) auto-retire-on-publish (GAP-20): with v1 and v2 both published the FIRST-REGISTERED wins on `resolveTemplate`'s tie-break — decide policy or leave to explicit retire; (6) retire and submit have no operator surface (GAP-25) — CLI verb, authoring surface, or "code is the surface" as policy; (7) regenerate mint-key suffix is a UUID nonce, not `reprintLogId` (append-only log must carry the NEW docId, which doesn't exist until after the mint) — confirm against the ADR-007 v1.1 letter | each item names its gate-check evidence in ROADMAP Stage 5 annotations (tasks 2–5) and docs/GAP-REGISTER.md GAP-19/20/24/25 | 2026-08-29 | **closed** — maintainer ruled directly in chat, 2026-08-29: **all seven stated defaults ratified as policy**, unchanged: (1) through-the-lifecycle — GAP-19's S1 seed stays the sole bypass; (2) two pairings — submitter may publish their own approved change; (3) written — regenerate logs the REPRINT + reprint_log row even when composition then fails; (4) 5 min — `STRANDED_AFTER_MS` ratified as-is; (5) no auto-retire-on-publish — first-registered-wins tie-break stands, retire must be explicit (GAP-20 closed); (6) code is the surface — retire and submit stay code-only, no CLI/UI added (GAP-25 closed); (7) nonce confirmed correct against the ADR-007 v1.1 letter, no change needed. |
| GATE-S3-THESIS-CHECK | show the two-minute end-to-end demo (event → rule trace → render → email → archived artifact → complete audit trail) to 5 real operators; DoD is notes in `docs/PREMORTEM.md`, feeds Stage 4's C2 gate | the demo itself is fully built and live-verified this session — `POST /event` on a purchase-order event → 0.157s response with rule trace, real PDF/A-2b archived, byte-identical delivery to outbox, complete audit trail in the registry (see docs/SESSION-LOG.md 2026-08-28 entries and this session's live podman demo). What's needed from the maintainer is the human part: actually showing it to 5 real operators and writing up what they say | 2026-08-28 | open — this is the one Stage 3 task that cannot be done by Claude. **Stage-5 exception ratified 2026-08-29** by the maintainer in chat: the loop may proceed into Stage 5 (Governance) while this gate stays open (gap register GAP-13's "ratified exception" arm). The gate itself is NOT closed — the 5-operator demo + `docs/PREMORTEM.md` still remain. |
| GATE-VERAPDF-INSTALL | `brew install verapdf` on this Mac (Java 21 already present) | needed for Stage 2's "veraPDF joins the corpus gates" task — PDF/A-2b compliance claims require it passing in CI per docs/STANDARDS.md ("no compliance claim without a validator") | 2026-08-27 | **closed** — maintainer answered directly in chat 2026-08-27: "yes, install it." Installed via `brew install verapdf` (pulled openjdk, upgraded harfbuzz/little-cms2); `verapdf 1.30.0` on PATH. |
| GATE-TYPST-INSTALL | `brew install typst` on this Mac | needed for gate-5 typst row + RTL/CJK typst column | 2026-08-26 | **answered** — typst 0.15.1 found on PATH at tick 5, gate-5 typst row + RTL/CJK typst column filled in RESULTS.md |
| GATE-BURST-WINDOW | the stated bursting window (minutes for 8,000 docs) | RESULTS.md bursting table will be filled per candidate window | 2026-08-26 | **closed** — maintainer chose 30 minutes 2026-08-27. Both renderers clear it single-process without worker-pool fan-out (pdf-direct 18.6x margin, Typst 2.25x margin); defers fan-out investment until a real SLA demands tighter. spike/RESULTS.md §Bursting math |
| GATE-CARBONE | author po-template.odt + LibreOffice, or write "skip carbone" in INBOX | RESULTS.md gate-matrix carbone column; blocks ADR-000 draft | 2026-08-26 | **closed** — maintainer chose "skip carbone" 2026-08-26 (docs/INBOX.md), not benchmarked. Rationale: LibreOffice runtime weight conflicts with "never gold-plate a renderer"; CCL license unreviewed and deferred. ADR-000 draft proceeds on pdf-direct + typst evidence only; Path B/Carbone stays optional behind the Renderer seam per HLD.md, revisited only if a named user needs .odt/.docx authoring |
| GATE-S1-PREWORK | may the loop do path-independent Stage 1 tasks while Stage 0 is human-blocked? default NO | see docs/LOOP-PLAN.md | 2026-08-26 | **closed** — maintainer answered directly in chat 2026-08-27: "Yes, start Stage 1," full authorization, not scoped to a subset, logged verbatim in docs/INBOX.md (Open section, this session). Superseded by circumstance: Stage 0's own exit gate is already 4/4 PASS (ROADMAP.md, 2026-08-27) and ADR-000/ADR-001 are both Accepted, so Stage 1 start no longer needs an early-start exception — this answer is read as a general go-ahead for all Stage 1 tasks. One carve-out stands independent of this gate: the Path B (Carbone) Stage 1 task stays out of scope per ADR-000's own "do not build both speculatively" clause, not because of this gate. Decision remains with the maintainer. |
| GATE-RTL-SHAPING | **superseded 2026-08-26 (tick 5)** — original tick-3 wording ("pdf-lib does no bidi/GSUB shaping") was wrong, see docs/RESULTS.md correction note; real gaps are narrower: (1) pdf-lib's TrueType subsetter breaks on large composite-glyph CJK fonts — full-font-embed workaround costs ~5.7MB/font; (2) pdf-lib has no font-fallback chain, so any font missing Latin/digit glyphs (common for OS "companion" scripts fonts) silently renders `.notdef` boxes when mixed with numbers — the normal shape of a real invoice. Typst has neither gap here. Decide: assemble complete-per-script font sets by hand for pdf-lib (no fallback), accept the CJK size cost, name Typst/a second renderer for non-Latin volume docs, or defer non-Latin scripts out of Stage 0 scope | docs/RESULTS.md § RTL / CJK smoke test (includes the correction writeup); `spike/` was deleted at Stage 0 close, evidence now lives only in docs/RESULTS.md — no longer independently reproducible via `npm run spike:rtl-cjk` | 2026-08-27 | **closed** — answered indirectly via ADR-001 (Accepted 2026-08-27), not a direct INBOX ruling on this gate ID. ADR-001 routes any document with non-Latin scripts to Typst (alongside multi-page layout and carry-forward totals), citing this gate's own evidence (no font-fallback chain, TrueType subsetter bug on composite-glyph CJK fonts) as the driver — this is option 3 ("name Typst/a second renderer for non-Latin volume docs"), superseding options 1/2/4. pdf-direct is not expected to handle non-Latin scripts going forward; no per-font-set assembly or CJK size-cost acceptance work is needed. Chair inference from ADR-001's text, not an explicit gate-ID citation by the maintainer — flagged here for the record. Decision remains with the maintainer. |
