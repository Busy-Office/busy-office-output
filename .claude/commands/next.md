---
description: Propose the single next task from the roadmap with its definition of done
---
Read ROADMAP.md, docs/SESSION-LOG.md, and ADRs/README.md.

1. Identify the current stage (first stage with unchecked tasks whose entry
   conditions are met).
2. If the stage's ADRs block progress, the next task is preparing the ADR
   decision (evidence gathering), not product code — say so explicitly.
3. Propose exactly ONE task: what, which files, which agent to delegate to,
   and its definition of done (the command or observable that proves it).
4. If the previous session left something half-open in SESSION-LOG.md, that
   item wins unless the human says otherwise.

Do not start the task. End with: "Proceed? (or name a different task)"
